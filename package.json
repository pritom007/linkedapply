{
  "name": "linked-apply",
  "version": "1.0.0",
  "description": "Chrome Extension to generate ATS-friendly CVs and cover letters from LinkedIn job postings",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:integration": "vitest run -c vitest.integration.config.ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3",
    "vite": "^7.3.1",
    "vite-plugin-static-copy": "^3.2.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "jspdf": "^4.2.0",
    "groq-sdk": "^0.3.0"
  }
}
